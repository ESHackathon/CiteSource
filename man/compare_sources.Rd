% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare_sources}
\alias{compare_sources}
\title{Compare duplicate citations across sources, labels, and strings}
\usage{
compare_sources(
  unique_data,
  comp_type = c("sources", "strings", "labels"),
  include_references = FALSE
)
}
\arguments{
\item{unique_data}{from ASySD, merged unique rows with duplicate IDs}

\item{comp_type}{Specify which fields are to be included. One or more of "sources", "strings" or "labels" - defaults to all.}

\item{include_references}{Should bibliographic detail be included in return?}
}
\value{
dataframe with indicators of where a citation appears, with sources/labels/strings as columns
}
\description{
Compare duplicate citations across sources, labels, and strings
}
\examples{
# Create sample data
sample_data <- data.frame(
  duplicate_id = c(1, 1, 2),
  cite_source = c("A", "B", "A"),
  cite_label = c("label_1", "label_2", "label_1"),
  cite_string = c("string_1", "string_2", "string_1"),
  record_ids = c("rec_1", "rec_2", "rec_3")
)

# Run compare_sources on sample data
result_compare <- compare_sources(sample_data)
result_compare_with_references <- compare_sources(sample_data, include_references = TRUE)
result_compare_sources_labels <- compare_sources(sample_data, comp_type = c("sources", "labels"))
result_compare_strings_labels <- compare_sources(sample_data, comp_type = c("strings", "labels"))
}
