% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_count_and_table.R, R/tables.R
\name{precision_sensitivity_table}
\alias{precision_sensitivity_table}
\title{Precision and Sensitivity Table}
\usage{
precision_sensitivity_table(data)

precision_sensitivity_table(data)
}
\arguments{
\item{data}{A data.frame. The dataset to build the table from.
It should contain the columns 'screened', 'final', 'Precision', 'Recall'.}
}
\value{
A \code{gt} table object summarizing the precision and sensitivity
metrics for each citation source, with relevant footnotes and labels.

A gt object representing the table.
}
\description{
This function generates a formatted table that displays the precision
and sensitivity (recall) metrics for each citation source, along with
distinct records and phase-specific counts such as "screened" and "final".

This function creates a gt table from a given data, and
removes the 'screened' column and its associated footnotes if all its values are zero.
}
\details{
The function first checks whether all values in the \code{screened} column are zero.
If so, the column is removed from the table. The table is then generated
using the \code{gt} package, with labeled columns and footnotes explaining the metrics.
}
\examples{
# Example usage with a sample dataset
sample_data <- data.frame(
  Source = c("Source1", "Source2", "Total"),
  Distinct_Records = c(100, 150, 250),
  final = c(80, 120, 200),
  Precision = c(80.0, 80.0, 80.0),
  Recall = c(40.0, 60.0, 100.0),
  screened = c(90, 140, 230)
)
precision_sensitivity_table(sample_data)
}
