% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{count_unique}
\alias{count_unique}
\title{Count number of unique and non-unique citations from different sources, labels, and strings}
\usage{
count_unique(unique_data, include_references = FALSE)
}
\arguments{
\item{unique_data}{from ASySD, merged unique rows with duplicate IDs}

\item{include_references}{Should bibliographic detail be included in return?}
}
\value{
dataframe with indicators of where a citation appears, with source/label/string as column
}
\description{
Count number of unique and non-unique citations from different sources, labels, and strings
}
\examples{
# Create sample data
sample_data <- data.frame(
  duplicate_id = c(1, 1, 2),
  cite_source = c("A", "B", "A"),
  cite_label = c("label_1", "label_2", "label_1"),
  cite_string = c("string_1", "string_2", "string_1"),
  record_ids = c("rec_1", "rec_2", "rec_3")
)

# Run count_unique on sample data
result_count <- count_unique(sample_data)
result_count_with_references <- count_unique(sample_data, include_references = TRUE)
}
